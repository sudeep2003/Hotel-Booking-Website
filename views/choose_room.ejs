<%- include("./partial/header") %>
<div class="container">
    <div class="row">
      <div class="col">
        <h1>Choose a room</h1>
  
        <ul>
          <% for(let a = 0; a<rooms.length; a++) { %>
            <li><a href="/choose-room/<%= rooms[a].room._id %>" class="link-primary"><%= rooms[a].room.roomName %></a></li>
          <% } %>
        </ul>
      </div>
    </div>
  </div>
<%- include("./partial/footer") %>
  
